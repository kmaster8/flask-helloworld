apiVersion: spec.keptn.sh/0.2.3
kind: "Shipyard"
metadata:
  name: "jes-example"
spec:
  stages:
    - name: "production"
#      sequences:
#      - name: "example-seq"
#        tasks:
##        - name: "remote-task"
